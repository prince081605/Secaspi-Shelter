# SECASPI SHELTER - Website Development TODO

## Phase 0: Foundation
- [x] Verify project structure (Laravel + React) and confirm dev commands
- [x] Implement authentication (Login/Register/Forgot/Email verification/Logout)
  - [x] Login/Register basic endpoints (UI + API wiring)
  - [x] Forgot password
- [x] API auth session/token basics (`/api/login`, `/api/user`, `/api/logout`)
  - [x] Implement `/api/user` (`AuthController@me`)
  - [ ] Email verification (`EmailVerificationController` and `EnsureEmailVerified` middleware exist, but neither is wired into any route yet)
- [x] Logout/session handling

- [ ] Implement role-based access: user / admin / volunteer (`User` model has a `role` field, but no middleware/gates are registered — only a placeholder `app/Http/Middleware/dummy.php` exists)
- [x] Add API base client + route guards in React
  - [x] API client (lib/api.js, lib/auth.js)
  - [x] RequireAuth wrapper in AppRouter.jsx
- [x] Add initial navigation/layout for public + protected areas
  - [x] Public routes: /, /login, /register, /forgot-password, /adopt

## Phase 1: Public - Home
- [x] Build Home page sections
  - [x] Full LandingPage.jsx with Hero, Stats, Featured Animals, How It Works, Rescue Report, Donate CTA, Footer (note: the originally-planned standalone "Success Stories" and "Volunteer CTA" sections were never built — the footer's `#about` anchor stands in for an About section)
  - [x] Responsive design with media queries, incl. a working mobile hamburger nav (added 2026-06-20)
  - [x] API integration via publicHomeApi.js
  - [x] Full-screen responsive layout (viewport width units)
  - [x] Redesigned from the original dark neon/glassmorphism theme to a clean, light, professional theme — Inter font, restrained green accent, soft shadows instead of glow effects (2026-06-20)
  - [x] Fixed bugs found during redesign: dark-on-dark heading contrast (leftover Vite scaffold CSS), capped page width with stray border lines, dead donate button, hardcoded copyright year, silent swallowed fetch error (2026-06-20)
- [x] Wire up dynamic stats/featured animals endpoints
  - [x] `getFeaturedAnimals()` / `/api/home/featured-animals` and `/api/home/stats` implemented in `api_public.php`
  - [x] Fixed a duplicate-photo-row data bug that caused every animal to render twice (React "duplicate key" warning) — cleaned up the duplicated `animal_photos` rows and hardened the query with `ROW_NUMBER()` so it can't recur (2026-06-20)

## Phase 2: Public - Browse Animals (completed 2026-06-20)
- [x] DB tables/models for animals and photos — added `Animal`, `AnimalPhoto`, `MedicalRecord`, `Vaccination` Eloquent models
  - Discovered along the way: the live DB schema doesn't match the migration files at all (every table actually uses `id` as its primary key, not the custom names like `animal_id`/`user_id`/`donation_id` the migrations claim). Fixed `User`/`Donation` models and `AuthController`/`DonationController` to match reality.
  - Also fixed: `User`/`Donation` had `$timestamps = true` (Eloquent default) but their tables only have `created_at`, no `updated_at` — broke `register()` outright. The `personal_access_tokens` table didn't exist at all (no migrations had ever been run against this DB) — ran just that one migration, which also means `createToken()`/Sanctum auth was completely broken before now.
- [x] Animal browse API: search + filters — `GET /api/animals` (`AnimalController@index`) with `q` (name/species/breed search), `species`, `gender`, `size`, `status`, paginated 12/page; `Adoption.jsx` now calls it (debounced) instead of client-side filtering
- [x] Animal detail page API + UI — `GET /api/animals/{id}` (`AnimalController@show`) returns photos + medical_records + vaccinations; `AnimalDetail.jsx` (`/adopt/:id`) renders a gallery, info, and combined medical history
- [x] Adoption form UI + create adoption application API — `POST /api/animals/{id}/adopt` (`AdoptionApplicationController`) + `AdoptionApply.jsx` (`/adopt/:id/apply`, behind `RequireAuth`)
- [x] Foster form UI + create foster application API — `POST /api/animals/{id}/foster` (`FosterApplicationController`) + `FosterApply.jsx` (`/adopt/:id/foster`, behind `RequireAuth`)
- [x] Fixed a blocking prerequisite: the frontend never stored or sent the Sanctum bearer token (`lib/auth.js` discarded it, `lib/api.js` never attached an `Authorization` header), so every `auth:sanctum` route — including `/api/donations` from Phase 3 — was unreachable. Now `lib/api.js` stores the token in `localStorage` and attaches it on every request; `RequireAuth` works correctly for the first time.
- Verified end-to-end via a real headless-browser run: register → login (token persisted) → browse with filters → animal detail → adoption application → foster application, with the resulting rows checked directly in the DB and the Laravel log confirmed clean on the happy path.

## Phase 3: Public - Donations (GCash)
- [x] DB tables/models for donations — `donations` table (from the initial migration) + `Donation` Eloquent model added (2026-06-20); no separate receipts table/model yet
- [x] Donation initiation UI + backend endpoint (create donation + payment ref) — `POST /api/donations` (`DonationController@store`, built earlier) + new `Donate.jsx` (`/donate`, behind `RequireAuth`): amount presets + custom input, payment method select, conditional GCash proof-image upload, submits as `FormData`, shows the resulting `reference_no` on success. The landing page's Donate button now navigates to `/donate` with the chosen amount pre-filled via router state instead of showing a placeholder alert (2026-06-20)
- [x] Payment verification endpoint (update donation status) — `POST /api/donations/{donation}/verify` (`DonationController@verify`), admin-role-gated (inline `role === 'admin'` check, matching the rest of the codebase), validates `status: in:verified,rejected`. Backend-only by design: there's no admin portal yet anywhere in the app (that's Phase 6), so no UI was built for this in isolation — verified directly via `curl` with an admin-promoted test user's token
- [x] Receipt generation UI + backend receipt data — `GET /api/donations/{donation}` (`DonationController@show`, 403s if the donation isn't the caller's and the caller isn't an admin) + new `Receipt.jsx` (`/donations/:id`, behind `RequireAuth`): reference/amount/method/status/date, `window.print()` button, print stylesheet hides nav/actions
- [x] Donation history UI + endpoint — `GET /api/donations` (`DonationController@index`, the caller's own donations, newest first, paginated) + new `DonationHistory.jsx` (`/donations`, behind `RequireAuth`): table with status badges and pagination, links to each receipt
- [x] Fixed a blocking prerequisite: `lib/api.js`'s `request()` unconditionally `JSON.stringify`'d the body and forced `Content-Type: application/json`, which breaks `multipart/form-data` uploads. It now detects `body instanceof FormData` and passes it through untouched (browser sets the boundary) — needed for the GCash proof-image upload (2026-06-20)
- Verified end-to-end via a real headless-browser run: register → login → landing page Donate button (amount carried via router state) → `/donate` → submit GCash donation with a real image file → success screen with reference no. → `/donations` shows the new row → its receipt at `/donations/:id` renders correctly. Confirmed the row directly in the DB (`status: pending`, correct user/amount/method, proof image saved to `storage/app/public/donations/`) and that `storage/logs/laravel.log` stayed clean on the happy path (2026-06-20)

## Phase 4: Public - Report Animal
- [x] DB tables/models for rescue/reports — `rescue_reports` table (from the initial migration) + new `RescueReport` Eloquent model (2026-06-20). No separate photos/status-history tables exist or were needed — `photo_url` is a single column (same shape as `Donation::proof_image`) and status is just a column update on the same row, mirroring how donations were modeled in Phase 3
  - Found and fixed a real bug along the way: unlike every other timestamp-less table in this DB, `rescue_reports.created_at` has no `DEFAULT CURRENT_TIMESTAMP` and is nullable — so with `$timestamps = false` it was silently saving as `NULL` on every insert. Fixed by explicitly setting `created_at` in `RescueReportController@store` (and adding it to the model's `$fillable`)
- [x] Report submission UI (reporter info, animal description, location, photo upload) — the landing page's existing Rescue section form now has a real photo upload field and submits a `FormData` to the backend via `lib/rescueApi.js`'s `createReport()`, with an inline success/error message (no more `alert()`) and a disabled/loading submit button while in flight. The "Condition" dropdown keeps its existing user-facing labels; they're mapped to the backend's `urgency` enum (`low/medium/high/critical`) before sending
- [x] Backend endpoints to store report — `POST /api/rescue-reports` (`RescueReportController@store`, public, no login required — matches the form being anonymous), validates location/urgency, uploads the optional photo, creates the report with `status: pending`, logs failures
- [x] Admin status update hooks — `GET /api/rescue-reports` (list, paginated) and `POST /api/rescue-reports/{report}/status` (update to pending/assigned/in_progress/resolved), both admin-role-gated. Backend-only by design, same scope boundary as `Donation::verify()` in Phase 3: there's no admin portal anywhere in the app yet (that's Phase 6), so no UI was built for these in isolation — verified directly via `curl` with an admin user's token (200) and a non-admin token (403)
- Verified end-to-end via a real headless-browser run: loaded the landing page, filled the rescue form with a real image file attached, submitted, confirmed the inline success message and form reset. Confirmed the row directly in the DB (correct urgency mapping, `created_at` now populated, photo saved to `storage/app/public/rescue-reports/`) and that `storage/logs/laravel.log` stayed clean on the happy path (2026-06-20)

## Phase 5: User Dashboard + Profile
- [x] Dashboard UI: my applications, my donations — `Dashboard.jsx`'s user-tab "My Applications" table and stat cards now render real data: merged adoption + foster applications for the logged-in user (`GET /api/adoption-applications`, `GET /api/foster-applications`, both new `index()` methods scoped to `request->user()->id`, mirroring `Donation::index()`), with animal name/thumbnail, status, and submission date; the four overview stat cards (total/approved/pending/rejected) are now computed from this real list instead of `dashboardMock.stats.user`. "My donations" was already real from Phase 3 (`/donations`) — nothing to do there
  - Notifications / saved animals / upcoming visits left out of scope — no backing tables exist anywhere in this DB (no favorites table, no notifications table, no visit-scheduling table; visit tracking is listed under Phase 6's admin adoption workflow). `UserFavorites`/`UserMessages` (which actually render global mock "recently adopted"/"medical reminders" data, not anything per-user) are left as mock placeholders
  - Admin tab (animal management, adoption requests, user management, analytics) intentionally untouched — still mock, that's Phase 6 (Admin Portal) scope
- [x] Profile UI: personal info + change password — `UserProfile` now shows the real `full_name`/`email`/`phone`, with an "Edit profile" form (`PUT /api/profile`, new `ProfileController@update`) and a separate "Change password" form (`POST /api/profile/change-password`, new `ProfileController@changePassword`, verifies the current password via `Hash::check` before allowing the change), both with inline success/error states
  - "Address management" left out of scope — the `users` table has no address column at all (only `full_name`/`email`/`phone`), same kind of schema gap as Phase 4's lat/lng columns
- [x] Backend endpoints for dashboard/profile — see above; also extended `AuthController::me()`'s response with `phone` for consistency (the dashboard's role check actually calls the raw `/api/user` Sanctum route, which already serialized the full model including `phone`)
- Verified end-to-end via a real headless-browser run: registered → logged in → submitted a real adoption application → `/dashboard` showed it in "My Applications" with correct status, and the stat cards reflected the real counts (1 total, 1 pending) → edited profile name/phone and confirmed it persisted after a page reload → changed password and confirmed the new password logged in successfully → confirmed the profile change in the DB and that `storage/logs/laravel.log` stayed clean (2026-06-20)

## Phase 6: Admin Portal (CRUD & Workflows) (completed 2026-06-21)
- [x] Animal management CRUD + archive + photo upload + status management — `AnimalController` gained `store/update/archive/destroy/addPhotos/destroyPhoto/adminIndex/adminShow` (all admin-gated); `destroy()` blocks deletion (409) when medical/vaccination/adoption/foster records exist, since the live DB's FKs are all `ON DELETE CASCADE` (discovered the hard way — see note below). New `AnimalsAdmin.jsx` replaces the mock animals grid on `/dashboard`'s Animals tab with real CRUD + a per-animal photo manager.
  - **Incident**: while verifying the delete guard, an early version of `destroy()` (which only caught DB exceptions, assuming `RESTRICT` FKs) cascade-deleted a real seed animal ("Buddy", id 1) plus its 1 medical record and 2 adoption applications. Fixed by explicitly checking for dependent rows before allowing delete. Per user direction, the lost seed data was not recreated.
- [x] Adoption workflow: review, home visit tracking, approval/decline, completion — new migration adds `home_visit_status/date/notes` to `adoption_applications`; `AdoptionApplicationController` gained `adminIndex`/`adminUpdate` (admin-gated). `AdoptionRequestsAdmin.jsx` wired into the Adoption Requests tab: status filter, approve/decline/mark-completed actions, expandable detail row with a home-visit scheduling form.
- [x] Foster workflow: applications → assignment → monitoring → completion — `FosterApplicationController` gained `adminIndex`/`adminUpdate` (no new columns needed; existing `status`/`start_date`/`end_date`/`notes` already cover the workflow). New `FosterRequestsAdmin.jsx` + new "Foster Requests" admin nav tab: status filter, approve → start fostering → mark completed actions, expandable monitoring panel (dates + notes).
- [x] Medical records: vaccinations/deworming/surgeries/logs/expenses/reminders — new `MedicalRecordController` and `VaccinationController` (admin-gated, nested under `/api/animals/{animal}/medical-records` and `/vaccinations`); reused the existing `adminShow` payload (already returned both lists) instead of adding new list endpoints. Wired into `AnimalsAdmin.jsx` as a new "Medical" panel per animal card (mirrors the Photos panel): tables + inline add forms for both records and vaccinations, delete buttons on each row. "Expenses/reminders" left as just the existing `cost`/`next_due` fields — no separate reporting view built yet (that's Phase 7/8 scope).
- [x] Rescue reports: pending → assigned team → notes → completion — new migration adds `assigned_to` (free-text team/person name — no admin/volunteer directory exists yet to pick a real user from) and `admin_notes` to `rescue_reports`; `RescueReportController::index()` gained a `status` filter, `updateStatus()` now accepts `assigned_to`/`admin_notes` alongside `status` (same route, no new endpoints needed since both were already admin-gated from Phase 4). New `RescueReportsAdmin.jsx` + "Rescue Reports" admin nav tab: status filter, expandable triage panel (assign + notes + one-click next-status action pending→assigned→in_progress→resolved).
- [x] Donation management: verify payments, fund allocation, analytics, financial reports — `DonationController` gained `adminIndex` (all donations, status filter, donor info) and `adminStats` (counts + totals by status, verified total by payment method); `verify()` already existed from Phase 3. New `DonationsAdmin.jsx` + "Donations" admin nav tab: stat cards (verified/pending totals, rejected count, GCash total), status filter, table with a proof-image link and Verify/Reject actions on pending rows. Deeper "financial reports" (exports, date-range breakdowns) deferred to Phase 7 (Reports & Exports) where they belong.
- [x] User management: roles, suspension — new `UserController` with `adminIndex` (search by name/email, filter by role/status) and `adminUpdate` (change role or status); includes a self-protection guard (admin can't demote/suspend their own account — returns a 422 with a clear message instead of risking lockout). New `UsersAdmin.jsx` replaces the mock Users tab table: search + role/status filters, inline role dropdown per row, Suspend/Reactivate toggle, "(you)" marker with disabled controls on the logged-in admin's own row.
- [x] Admin dashboard metrics + activity feed — new `DashboardController::adminOverview()` aggregates real counts (animals in care, adopted this year, ready for adoption, donations this month), an animal-status breakdown for the analytics bars, and a merged/sorted recent-activity feed across rescue reports, donations, and adoption/foster applications (top 10). Wired into `Dashboard.jsx`: the top `OverviewCards` (admin tab) now read real `overview.stats` instead of `dashboardMock.stats.admin`; the Reports tab's `AdminAnalytics` now renders real `overview.analytics` instead of `dashboardMock.analyticsBars`; the Dashboard nav's home view now shows a new `ActivityFeed` instead of a duplicate of the Animals tab. "Admin auth" was already done in Phase 0 (the `admin` middleware) — nothing left to do there.
- [x] Volunteer management: profiles, task assignment, hours, attendance, reports — **discovered the `volunteers`/`volunteer_tasks` tables don't exist at all** in the live DB despite being defined in the original migration (that migration shows as "Pending" — it was never actually run; the live schema came from a separate manual import that omitted them, same root cause as the Phase 2 PK-naming mismatch). Added a new standalone migration creating both tables Laravel-natively (`id` PK, `created_at` only, matching live conventions) with real FK constraints using MySQL's default RESTRICT behavior (not CASCADE) specifically to avoid a repeat of the Animal-delete data-loss incident above. New `Volunteer`/`VolunteerTask` models, `VolunteerController` (admin-gated: list/create/update/delete volunteer profiles + assign/update/delete tasks). Creating a volunteer profile sets the linked user's `role` to `volunteer` (skipped for admins); deleting one reverts it back to `user`. Deletion is blocked (409) while a volunteer has any tasks, mirroring the Animal guard. New `VolunteersAdmin.jsx` + "Volunteers" admin nav tab: search-and-pick existing users to onboard, hours-rendered tracking, expandable per-volunteer task panel with status progression (assigned→ongoing→completed). "Attendance" and "reports" not built as separate views — hours_rendered + task status stand in for them; deeper reporting is Phase 7/8 scope.
- [x] Intake management (rescue/owner surrender/stray) + documents + assessment — built as a genuinely new module per explicit choice, distinct from `rescue_reports`. New `intakes` + `intake_documents` tables (Laravel-native migration, `id` PKs); `intake_documents.intake_id` cascades on delete (a pure owned sub-resource, same as `animal_photos`), but `intakes.converted_animal_id` is a plain unconstrained column — no FK at all — so neither deleting an intake nor deleting the animal it produced can ever block or cascade unexpectedly. New `Intake`/`IntakeDocument` models, `IntakeController` (admin-gated: list/create/show/update/delete intakes, document upload/delete, and a dedicated `convert` action that creates a real `Animal` record — defaulting its status to `quarantine` for a post-intake health check — and locks the intake's status to `converted`; direct attempts to set `status: converted` via the regular update endpoint are rejected with a 422 telling the caller to use `/convert` instead).
  - **Update (2026-06-21)**: merged into the Animals module per request — the standalone "Intakes" admin nav tab and `IntakesAdmin.jsx` page were removed; the same filters/new-intake form/expandable assessment panel now render as an "Intake Queue" section directly above the animal grid inside `AnimalsAdmin.jsx`. Each intake row also gained a one-click "Add to Animals" button (wrapping the existing `convert` action) so converting no longer requires expanding the row first; backend `IntakeController`/`intakesApi.js` untouched.
- [x] Animal QR codes (2026-06-21) — each animal now gets a unique QR code automatically. New `endroid/qr-code` composer dependency (SVG output, no GD/Imagick dependency) and a nullable `qr_code_path` column on `animals`. `AnimalController::ensureQrCode()` generates an SVG encoding that animal's public `/adopt/{id}` page URL (base URL from a new `frontend_url` config / `FRONTEND_URL` env var) and stores it on the same `public` disk as photos; called from `store()` so brand-new animals get one immediately, and from `show()`/`adminShow()` so any animal that already existed gets one lazily generated the first time it's viewed — no backfill script needed. Displayed on the public `AnimalDetail.jsx` page (below the photo gallery, with a "Download QR" link) and in `AnimalsAdmin.jsx` via a new per-card "QR Code" toggle button (alongside Photos/Medical).
- **Phase 6 complete** — all 10 sub-phases of the Admin Portal are done and backend-verified end-to-end via authenticated `curl` runs (no browser-automation tool was available in this environment to verify the UI visually; see the verification note under Animal management above). The two 2026-06-21 follow-ups above were additionally verified visually via headless-browser screenshots.

## Phase 7: Reports & Exports (completed 2026-06-21)
- [x] Reports API: adoption/animal/medical/donation/volunteer/rescue — new `ReportController` with one JSON endpoint per domain (`GET /api/admin/reports/{adoption,animals,medical,donations,volunteers,rescue}`, all admin-gated), each returning a generic `{summary, columns, rows}` shape (summary = stat chips, columns/rows drive a single reusable frontend table). Filters per domain: date range (`from`/`to`) on adoption/medical/donations/rescue, `status` on adoption/animals/donations/rescue, plus domain-specific extras (`species` on animals, `payment_method` on donations, `urgency` on rescue, `record_type` on medical). Medical report merges `MedicalRecord` + `Vaccination` rows into one timeline. Mirrors the existing `DashboardController::adminOverview()` / `DonationController::adminStats()` aggregation conventions (groupBy + pluck/selectRaw+keyBy).
- [x] CSV export endpoint — `GET /api/admin/reports/export/csv?type=...` streams the same resolved data via `fputcsv`/`response()->streamDownload()`, columns as the header row, no new dependency.
- [x] PDF export endpoint — `GET /api/admin/reports/export/pdf?type=...` renders a new generic `resources/views/reports/pdf.blade.php` (driven entirely by `summary`/`columns`/`rows`, no per-domain template) via new `barryvdh/laravel-dompdf` dependency.
- New `ReportsAdmin.jsx` replaces the old static-mock `AdminAnalytics` bar-chart stub on the Reports admin nav tab: report-type selector, the filters above, summary stat cards, a generic data table, and Export CSV/PDF buttons. Since Sanctum auth is a Bearer header (not a cookie), plain `<a href>` downloads can't carry it — added `downloadFile()` to `lib/api.js` (fetch with the auth header, parse the filename from `Content-Disposition`, hand back a Blob) and a new `lib/reportsApi.js` wrapping both the JSON and export calls.
- Verified backend end-to-end via authenticated `curl`: all 6 JSON endpoints return correct live-DB data, CSV export streams valid rows, PDF export produces a valid file, and an invalid `type` correctly 422s. Frontend verified via a clean `vite build` (no browser-automation tool available in this environment to screenshot the UI visually).

## Phase 8: Notification System (completed 2026-06-22)
- [x] In-app notifications schema + endpoints — new `app_notifications` table (named to avoid colliding with Laravel's `Notifiable`-trait-assumed polymorphic `notifications` table, since `User` already used that trait) + `Notification` model; `NotificationController` (`GET /api/notifications` paginated + unread_count, `POST /api/notifications/{id}/read`, `POST /api/notifications/read-all`), all `auth:sanctum`-gated, ownership-checked. New `NotificationBell.jsx` (bell icon + unread badge + dropdown, polls every 30s) wired into `Dashboard.jsx`'s top row — not explicitly listed in this checklist item, but a notification system needs a visible surface to be usable.
- [x] Email notifications for key events — new `App\Notifications\AppNotification` abstract base (single source of truth per event: `type()/title()/message()/data()`, sent over both the in-app row and Laravel's `mail` channel via `sendTo($user)`) plus 4 concrete events: `AdoptionStatusChanged`, `FosterStatusChanged`, `DonationStatusChanged`, `VolunteerTaskNotification` (assigned + status-updated). Wired into `AdoptionApplicationController@adminUpdate`, `FosterApplicationController@adminUpdate`, `DonationController@verify`, `VolunteerController@storeTask`/`updateTask` — fires only when `status` actually changed (`wasChanged('status')`), not on every update. `RescueReportController` intentionally excluded: rescue reports have no `user_id` (anonymous submissions, per Phase 4), so there's no account to notify.
  - Scoped down from "SendGrid" to provider-agnostic Laravel mail (per explicit choice): no SendGrid account/API key exists in this environment (.env has `MAIL_MAILER=log`). The notification class uses Laravel's standard mail channel, so it already works end-to-end against the log driver and becomes real SendGrid delivery the moment `MAIL_MAILER`/SMTP credentials are set — no code change needed.
- SMS notifications dropped from scope entirely (per explicit choice) — no SMS gateway account/credentials exist in this environment.
- Verified end-to-end: backend via `curl` (issued a real Sanctum token, hit all 3 endpoints, confirmed pagination/unread_count/mark-read/mark-all-read) and via `tinker` (fired `AdoptionStatusChanged` against real seed data, confirmed both the `app_notifications` row and the rendered mail body in `storage/logs/laravel.log`). Frontend via a real headless-browser run (registered a throwaway user, seeded 2 notifications, logged in, opened the bell dropdown, clicked one to mark it read, clicked "Mark all read", confirmed the badge disappears and unread highlighting clears) — screenshots confirmed correct on-theme rendering. Full `php artisan test` suite (52 tests) still green after all wiring.
  - Migration gotcha hit again: `app_notifications.user_id` had to be a plain signed `integer` + manual `foreign()` (not `foreignId()`), because the live `users.id` is `int(11)`, not bigint unsigned — same errno 150 issue as the volunteers migration. Caught it by actually running `php artisan migrate` against the real dev DB (not just the sqlite test suite), which doesn't enforce column-type matching on FKs and would have hidden the bug.

## Phase 9: Hardening & Testing
- [ ] Add validation, pagination, rate limiting
- [ ] Add audit logs for admin actions
- [x] Add automated tests (API feature tests) — new `tests/Feature/AuthTest.php` (register/login/me/profile role-escalation guard/password reset, 7 tests) and `tests/Feature/AuthorizationTest.php` (data-provider matrix asserting every admin-gated route 401s for guests, 403s for non-admins, and lets admins through — 43 tests across 14 representative routes + 1 public-endpoint sanity check). 52/52 tests pass (`php artisan test`).
  - Needed two fixture fixes to make `RefreshDatabase` work at all: `UserFactory` was still defining Jetstream-scaffold columns (`name`, `email_verified_at`, `remember_token`) that don't exist on the real `users` table (`full_name`/`role`/`status`/`email_verified`); rewrote it to match reality, added `admin()`/`suspended()` states, and overrode `newModel()` to `forceFill` since `User` guards `role`/`status`/`email_verified` against mass assignment. Also fixed `2026_06_21_110000_create_volunteers_tables.php`'s `user_id` FK column type (plain `integer` mismatched `users.id`'s `bigint unsigned`, errno 150 on migrate) to `foreignId()->constrained('users')`.
  - Environment blocker found and fixed: `phpunit.xml` runs tests against sqlite `:memory:`, but `pdo_sqlite` was commented out in `C:\php\php.ini` ("could not find driver"). Enabled it (with explicit user confirmation, since it's a system-level php.ini edit outside the repo).
- [ ] End-to-end manual verification of critical flows
